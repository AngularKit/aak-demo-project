// Setup global des tests (exécuté avant chaque fichier de spec).
// Enregistre les matchers @testing-library/jest-dom sur l'`expect` de Vitest
// (toBeInTheDocument, toHaveTextContent, …).
import '@testing-library/jest-dom/vitest';
