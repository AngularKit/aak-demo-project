# Angular CLI attend un node_modules à plat.
node-linker=hoisted
# NgRx 21 déclare un peer Angular ^21 alors que le repo est en Angular 22
# (pas encore de NgRx pour la 22) — on tolère le décalage.
strict-peer-dependencies=false
auto-install-peers=true
