# Dépendances autorisées à exécuter leurs build scripts natifs
# (esbuild est requis par @angular/build ; les autres accélèrent build/watch).
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  lmdb: true
  msgpackr-extract: true
